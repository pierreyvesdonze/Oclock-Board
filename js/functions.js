//////////////////////////////////////////////////// RotateStyle
// Fonction permettant de changer le css
function swapStylesheet(sheet) {
    document.getElementById('cssFile').setAttribute('href', sheet)
}